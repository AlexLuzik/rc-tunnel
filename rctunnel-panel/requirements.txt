# Pins bumped for security advisories (2nd security review):
#  - starlette >=0.49.1 fixes the Range-header / multipart DoS; FastAPI is bumped
#    so its transitive starlette pin permits it (and starlette is pinned explicitly).
#  - cryptography >=44 picks up X.509 / OpenSSL fixes beyond the 43.x line.
fastapi>=0.119,<1.0
starlette>=1.0.1            # >=1.0.1 also fixes the Host-header path-desync (middleware authz)
uvicorn[standard]>=0.32,<1.0
sqlalchemy==2.0.*
pydantic[email]==2.*
pydantic-settings==2.*
jinja2>=3.1.6,<4
python-multipart>=0.0.18,<1.0
argon2-cffi==23.*
pyjwt>=2.10,<3
websockets>=13,<16
alembic==1.*
cryptography>=44,<47
psycopg[binary]==3.*
